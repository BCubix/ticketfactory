import styled from "@emotion/styled";
import { IconButton } from "@mui/material";

export const MoveElementButton = styled(IconButton)`
    color: ${(props) => props.theme.palette.secondary.main};
    margin-left: 3px;
`
