import { Box } from '@mui/system';
import React from 'react';

export const UploadFileContent = ({
    value,
    label,
    handleChange,
    handleBlur,
    name,
    error,
    field,
    ...rest
}) => {
    console.log(field);
    return <Box></Box>;
};
